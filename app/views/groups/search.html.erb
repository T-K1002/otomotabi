<% if @group.present? %>
  <%= @group.name %>に参加しますか</td>
  <%= link_to '加入申請', group_permits_path(@group), data: { turbo_method: :post }  %>
<% else %>
  <div>該当するグループが存在しません</div>
<% end %>

<h2>もう一度グループを探す</h2>
<%= form_with url: search_path, local: true, method: :get do |f| %>
    <%= f.text_field :name %>
    <%= f.submit "検索" %>
  <% end %>


   


 
   
     
